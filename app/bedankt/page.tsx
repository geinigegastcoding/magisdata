import { LandingRoutePage, landingRouteMetadata } from "@/components/landing-route-page";

export const metadata = landingRouteMetadata("bedankt");

export default function Page() {
  return <LandingRoutePage slug="bedankt" />;
}
